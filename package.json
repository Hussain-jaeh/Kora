{
  "name": "whatsapp-business-os",
  "version": "0.1.0",
  "private": true,
  "description": "WhatsApp-first assistant + business OS for small physical businesses. server/ = WhatsApp socket + REST API, web/ = owner dashboard.",
  "scripts": {
    "setup": "cd server && npm install && cd ../web && npm install",
    "migrate": "cd server && npm run migrate --",
    "server": "cd server && npm start",
    "web": "cd web && npm run dev"
  }
}

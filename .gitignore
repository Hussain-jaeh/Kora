# Secrets — never commit these
.env
.env.local

# Live WhatsApp session (leaking this = anyone can send as the business)
auth/

# Dependencies & build output
node_modules/
dist/
.vite/

# Noise
.DS_Store
*.log
npm-debug.log*

# Files the Claude desktop app drops here
Claude outputs/
